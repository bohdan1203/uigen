Write comprehensive tests for: $ARGUMENTS

Testing conventions:

- Use Vitsts with React Testing Library
- Place test files in a **test** directory in the same falder as the source file
- Name test files as [filename],test.ts(x)
- Use @/ prefix for imports

Coverage:

- Test happy paths
- Test edge cases
- Test error states
- Focus on testing behavior and public API's rather than implementation details
